@model Counter

<div>
    <p class="badge badge-secondary">
        Count: <strong>@Model.Count</strong>
    </p>
    <button class="btn btn-circle btn-primary" hydro-on:click="@(() => Model.Add())">
        Add
    </button>
</div>